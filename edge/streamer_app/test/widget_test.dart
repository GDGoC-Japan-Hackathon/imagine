// Streamer アプリのスモークテスト
// カメラや WebSocket の実際の動作はデバイス上での実機テストで確認してください。

import 'package:flutter_test/flutter_test.dart';

void main() {
  // デバイス依存のテスト（カメラ・WebSocket）は実機でのみ実行可能です
  // ここでは最低限の構成確認のみ行います
  test('プレースホルダーテスト', () {
    expect(true, isTrue);
  });
}
